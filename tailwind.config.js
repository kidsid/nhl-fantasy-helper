const colors = require('./src/assets/teamColors');

module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    colors: {
      penguins: {
        DEFAULT: '#FEB81D'
      }
    },
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
